@model LTMSV2.Models.ParcelTypeVM

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_TrueBookMstr.cshtml";
}


<script type="text/javascript">
    $(document).ready(function () {
        $(":text").css({ "border-radius": "5px" });
        $("select").css({ "border-radius": "5px" });

  

    });
</script>

<section class="content-header">
    <h1 class="headingfont">Parcel Type Master - Modify</h1>

</section>

<section class="content">
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    
       <div id="validations" style="color:red;margin-left:7px;display:none">* Please fill mandatory fields</div>
    <fieldset>
            <div class="searchdiv sm-width">
           <div class="row no-margin"> 
               @Html.HiddenFor(model => model.id)
             <div class="col-md-12">
                          <label class="headinglabel required">Docs/Non Docs</label>
                        @* @Html.DropDownListFor(model => model.DescID, ViewBag.doctype as SelectList, new { @class = "form-control txttarget" })*@
                    @Html.DropDownList("DescID", new List<SelectListItem>
                {
                    new SelectListItem{ Text="Docs", Value = "0" },
                    new SelectListItem{ Text="Non Docs", Value = "1" },
                 
              
                   
                 },"Select", new { @class = "form-control" })
                 
                 
                 </div>
      

               <div class="col-md-12">
                          <label class="headinglabel required">Description</label>
                        @Html.TextBoxFor(model=>model.Description,new{@class="form-control"})          
             
     
                   </div>

         </div>

      <div class="row no-margin" style="padding-top:10px">
                        <div class="col-md-12 btn-right">
                           
                              <input type="submit" value="Update" class="btn btn-primary btnwidth" data-toggle="tooltip" title="Click here" id="btnsave" />
                               
                          
                        
                          <a href='@Url.Action("Index", "ParcelType", new { id = 0 })' class="btn btn-danger btnwidth" data-toggle="tooltip" data-placement="right" title="Click here" style="margin-left:5px;">Cancel</a>
                          
                        </div>
                    </div>

</div>
    </fieldset>
}
    </section>

